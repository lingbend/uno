
class Card:

    ## reverse, draw_2, wild, wild_draw_4, skip

    def __init__(self, color, type, deck, discard):
        self.color = color
        self.type = type
        self.deck = deck
        self.discard = discard

    def play_card(self, type): # is used by play_card function for player
        discard.add_card(self) # calls add card function on discard deck object
        
        if 
        # check type
        # if type not number, do action
